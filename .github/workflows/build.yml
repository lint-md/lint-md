name: build

on: [ pull_request ]

permissions:
  contents: read

jobs:
  unit-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6.0.2

      - name: Install modules
        run: yarn install

      - name: Run tests
        run: yarn test
